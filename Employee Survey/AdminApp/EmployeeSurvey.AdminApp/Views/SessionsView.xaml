<UserControl x:Class="EmployeeSurvey.AdminApp.Views.SessionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <DockPanel Margin="20">
        <TextBlock DockPanel.Dock="Top" Text="Live Session Monitoring" FontSize="24" FontWeight="Bold" Foreground="#1E293B"/>

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,15">
            <Button Content="Refresh" Padding="15,8" Background="#0EA5E9" Foreground="White" BorderThickness="0" Command="{Binding LoadSessionsCommand}"/>
            <Button Content="Terminate Session" Padding="15,8" Margin="10,0" Background="#EF4444" Foreground="White" BorderThickness="0" 
                    Command="{Binding TerminateSessionCommand}" CommandParameter="{Binding SelectedSession}"/>
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Foreground="#64748B" Margin="10,0"/>
        </StackPanel>

        <Border Background="White" CornerRadius="8" Padding="10">
            <DataGrid ItemsSource="{Binding Sessions}" SelectedItem="{Binding SelectedSession}"
                      AutoGenerateColumns="False" 
                      HeadersVisibility="Column" Background="Transparent" BorderThickness="0"
                      CanUserAddRows="False" IsReadOnly="True" GridLinesVisibility="None">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Employee" Binding="{Binding UserName}" Width="150"/>
                    <DataGridTextColumn Header="Test" Binding="{Binding TestTitle}" Width="*"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Status}" Value="Started">
                                        <Setter Property="Foreground" Value="#F59E0B"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Status}" Value="Completed">
                                        <Setter Property="Foreground" Value="#10B981"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Started At" Binding="{Binding StartAt, StringFormat=\{0:HH:mm dd/MM\}}" Width="120"/>
                    <DataGridTextColumn Header="Last Activity" Binding="{Binding LastActivityAt, StringFormat=\{0:HH:mm:ss\}}" Width="120"/>
                    <DataGridTextColumn Header="Score" Binding="{Binding TotalScore}" Width="80"/>
                    <DataGridTextColumn Header="% " Binding="{Binding Percent, StringFormat=\{0:F1\}%}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </DockPanel>
</UserControl>
