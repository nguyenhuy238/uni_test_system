<UserControl x:Class="EmployeeSurvey.AdminApp.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Welcome to Admin Dashboard" FontSize="24" FontWeight="Bold" Foreground="#1E293B"/>
            <TextBlock Text="Quick statistics and status overview will appear here." Margin="0,10,0,0" Foreground="#64748B"/>
            
            <UniformGrid Columns="3" Margin="0,30,0,0">
                <Border Background="White" Height="100" Margin="0,0,10,0" CornerRadius="8" Padding="15">
                    <StackPanel>
                        <TextBlock Text="Questions" Foreground="#94A3B8"/>
                        <TextBlock Text="{Binding Summary.TotalQuestions}" FontSize="24" FontWeight="Bold" Foreground="#0EA5E9"/>
                    </StackPanel>
                </Border>
                <Border Background="White" Height="100" Margin="10,0" CornerRadius="8" Padding="15">
                     <StackPanel>
                        <TextBlock Text="Total Users" Foreground="#94A3B8"/>
                        <TextBlock Text="{Binding Summary.TotalUsers}" FontSize="24" FontWeight="Bold" Foreground="#10B981"/>
                    </StackPanel>
                </Border>
                <Border Background="White" Height="100" Margin="10,0,0,0" CornerRadius="8" Padding="15">
                     <StackPanel>
                        <TextBlock Text="Submissions" Foreground="#94A3B8"/>
                        <TextBlock Text="{Binding Summary.TotalSubmissions}" FontSize="24" FontWeight="Bold" Foreground="#F59E0B"/>
                    </StackPanel>
                </Border>
            </UniformGrid>
        </StackPanel>
    </Grid>
</UserControl>
