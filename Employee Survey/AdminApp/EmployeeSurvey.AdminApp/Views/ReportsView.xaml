<UserControl x:Class="EmployeeSurvey.AdminApp.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <DockPanel Margin="20">
        <TextBlock DockPanel.Dock="Top" Text="Enterprise Reports &amp; Analytics" FontSize="24" FontWeight="Bold" Foreground="#1E293B"/>

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,15">
            <Button Content="Refresh Data" Padding="15,8" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Command="{Binding LoadResultsCommand}"/>
            
            <Separator Visibility="Hidden" Width="20"/>
            
            <Button Content="Export to Excel" Padding="15,8" Background="#10B981" Foreground="White" BorderThickness="0" Command="{Binding ExportExcelCommand}">
                <Button.ToolTip>Download results in .xlsx format</Button.ToolTip>
            </Button>
            
            <Button Content="Export to PDF" Padding="15,8" Margin="10,0" Background="#EF4444" Foreground="White" BorderThickness="0" Command="{Binding ExportPdfCommand}">
                <Button.ToolTip>Generate a PDF summary report</Button.ToolTip>
            </Button>

            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Foreground="#64748B" Margin="10,0"/>
        </StackPanel>

        <Border Background="White" CornerRadius="8" Padding="10">
            <DataGrid ItemsSource="{Binding Results}" 
                      AutoGenerateColumns="False" 
                      HeadersVisibility="Column" Background="Transparent" BorderThickness="0"
                      CanUserAddRows="False" IsReadOnly="True" GridLinesVisibility="None">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Employee" Binding="{Binding UserName}" Width="150"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding UserEmail}" Width="200"/>
                    <DataGridTextColumn Header="Test Title" Binding="{Binding TestTitle}" Width="*"/>
                    <DataGridTextColumn Header="Score" Binding="{Binding ScoreDisplay}" Width="100"/>
                    <DataGridTextColumn Header="Submit Date" Binding="{Binding SubmitTime, StringFormat=\{0:dd/MM/yyyy HH:mm\}}" Width="150"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </DockPanel>
</UserControl>
