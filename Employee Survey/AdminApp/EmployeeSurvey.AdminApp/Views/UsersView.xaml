<UserControl x:Class="EmployeeSurvey.AdminApp.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel Margin="20">
        <TextBlock DockPanel.Dock="Top" Text="User Directory" FontSize="24" FontWeight="Bold" Foreground="#1E293B"/>
        
        <Border Background="White" CornerRadius="8" Padding="10" Margin="0,20,0,0">
            <DataGrid ItemsSource="{Binding Users}" AutoGenerateColumns="False" 
                      HeadersVisibility="Column" Background="Transparent" BorderThickness="0"
                      CanUserAddRows="False" IsReadOnly="True" GridLinesVisibility="None">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                    <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="100"/>
                    <DataGridTextColumn Header="Department" Binding="{Binding Department}" Width="100"/>
                    <DataGridTextColumn Header="Team" Binding="{Binding Team}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </DockPanel>
</UserControl>
