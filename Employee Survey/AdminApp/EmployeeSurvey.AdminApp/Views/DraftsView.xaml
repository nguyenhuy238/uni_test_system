<UserControl x:Class="EmployeeSurvey.AdminApp.Views.DraftsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <DockPanel Margin="20">
        <TextBlock DockPanel.Dock="Top" Text="Offline Drafts Library" FontSize="24" FontWeight="Bold" Foreground="#1E293B"/>

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,15">
            <Button Content="Open Selected" Padding="15,8" Background="#0EA5E9" Foreground="White" BorderThickness="0" 
                    Command="{Binding OpenDraftCommand}" CommandParameter="{Binding SelectedDraft}"/>
            <Button Content="Delete" Padding="15,8" Margin="10,0" Background="#EF4444" Foreground="White" BorderThickness="0" 
                    Command="{Binding DeleteDraftCommand}" CommandParameter="{Binding SelectedDraft}"/>
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Foreground="#64748B" Margin="10,0"/>
        </StackPanel>

        <Border Background="White" CornerRadius="8" Padding="10">
            <DataGrid ItemsSource="{Binding Drafts}" SelectedItem="{Binding SelectedDraft}"
                      AutoGenerateColumns="False" 
                      HeadersVisibility="Column" Background="Transparent" BorderThickness="0"
                      CanUserAddRows="False" IsReadOnly="True" GridLinesVisibility="None">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100"/>
                    <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                    <DataGridTextColumn Header="Saved Date" Binding="{Binding CreatedAt, StringFormat=\{0:dd/MM/yyyy HH:mm\}}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </DockPanel>
</UserControl>
