<UserControl x:Class="EmployeeSurvey.AdminApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EmployeeSurvey.AdminApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="400">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">
            <TextBlock Text="Admin Login" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,10" Padding="8"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,10" Padding="8"/>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" Margin="0,10"/>
            <Button Content="Login" Command="{Binding LoginCommand}" Margin="0,10" Padding="8"/>
        </StackPanel>
    </Grid>
</UserControl>
