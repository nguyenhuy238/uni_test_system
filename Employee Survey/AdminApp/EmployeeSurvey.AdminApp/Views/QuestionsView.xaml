<UserControl x:Class="EmployeeSurvey.AdminApp.Views.QuestionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DockPanel Margin="20">
            <TextBlock DockPanel.Dock="Top" Text="Question Bank Management" FontSize="24" FontWeight="Bold" Foreground="#1E293B"/>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,15">
            <Button Content="New Question" Padding="15,8" Background="#0EA5E9" Foreground="White" BorderThickness="0" Command="{Binding CreateNewCommand}"/>
            <Button Content="Refresh" Padding="15,8" Margin="10,0" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Command="{Binding LoadDataCommand}"/>
            <Button Content="Delete Selected" Padding="15,8" Background="#EF4444" Foreground="White" BorderThickness="0" 
                        Command="{Binding DeleteQuestionCommand}" CommandParameter="{Binding SelectedQuestion}"/>
                <Button Content="Import Excel" Padding="15,8" Margin="0,0,10,0" Background="White" BorderThickness="1" BorderBrush="#CBD5E1"/>
            </StackPanel>
            
            <Border Background="White" CornerRadius="8" Padding="10">
                <DataGrid ItemsSource="{Binding Questions}" SelectedItem="{Binding SelectedQuestion}"
                          AutoGenerateColumns="False" 
                          HeadersVisibility="Column" Background="Transparent" BorderThickness="0"
                          CanUserAddRows="False" IsReadOnly="True" GridLinesVisibility="None"
                          MouseDoubleClick="DataGrid_MouseDoubleClick">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Content" Binding="{Binding Content}" Width="*"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100"/>
                        <DataGridTextColumn Header="Skill" Binding="{Binding Skill}" Width="100"/>
                        <DataGridTextColumn Header="Difficulty" Binding="{Binding Difficulty}" Width="100"/>
                        <DataGridTextColumn Header="Created" Binding="{Binding CreatedAt, StringFormat=d}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </DockPanel>
    </Grid>
</UserControl>
