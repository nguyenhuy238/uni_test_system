<UserControl x:Class="EmployeeSurvey.AdminApp.Views.AdminMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EmployeeSurvey.AdminApp.Views"
             xmlns:vm="clr-namespace:EmployeeSurvey.AdminApp.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.InputBindings>
        <KeyBinding Modifiers="Control" Key="N" Command="{Binding CurrentViewModel.CreateNewCommand, FallbackValue={x:Null}}"/>
        <KeyBinding Modifiers="Control" Key="S" Command="{Binding CurrentViewModel.SaveCommand, FallbackValue={x:Null}}"/>
        <KeyBinding Modifiers="Control" Key="R" Command="{Binding CurrentViewModel.LoadDataCommand, FallbackValue={x:Null}}"/>
        <KeyBinding Key="Escape" Command="{Binding CurrentViewModel.GoBackCommand, FallbackValue={x:Null}}"/>
    </UserControl.InputBindings>

    <Grid Background="#F4F7F9">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="#1E293B" CornerRadius="0">
            <StackPanel Margin="0,20,0,0">
                <TextBlock Text="ADMIN PANEL" Foreground="White" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30"/>
                
                <Button Style="{StaticResource SidebarButtonStyle}" Content="Dashboard" Command="{Binding NavigateToDashboardCommand}"/>
                <Button Style="{StaticResource SidebarButtonStyle}" Content="Question Bank" Command="{Binding NavigateToQuestionsCommand}"/>
                <Button Style="{StaticResource SidebarButtonStyle}" Content="Tests &amp; Groups" Command="{Binding NavigateToTestsCommand}"/>
                <Button Style="{StaticResource SidebarButtonStyle}" Content="Session Monitoring" Command="{Binding NavigateToSessionsCommand}"/>
                <Button Style="{StaticResource SidebarButtonStyle}" Content="Analytics &amp; Reports" Command="{Binding NavigateToReportsCommand}"/>
                <Button Style="{StaticResource SidebarButtonStyle}" Content="Drafts Library" Command="{Binding NavigateToDraftsCommand}"/>
                <Button Style="{StaticResource SidebarButtonStyle}" Content="Users &amp; Teams" Command="{Binding NavigateToUsersCommand}"/>
                
                <Separator Background="#334155" Margin="20,20"/>
                
                <TextBlock Text="{Binding StatusMessage}" Foreground="#94A3B8" FontSize="12" TextAlignment="Center" VerticalAlignment="Bottom"/>
            </StackPanel>
        </Border>

        <!-- Content Area -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top Header -->
            <Border Grid.Row="0" Background="White" Height="60">
                <Grid Margin="20,0">
                    <TextBlock Text="{Binding CurrentViewModel.Title}" VerticalAlignment="Center" FontSize="18" FontWeight="SemiBold" Foreground="#334155"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock Text="Administrator" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#64748B"/>
                        <Ellipse Width="35" Height="35" Fill="#38BDF8"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Module Content -->
            <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}" Margin="20"/>
        </Grid>
    </Grid>
</UserControl>
