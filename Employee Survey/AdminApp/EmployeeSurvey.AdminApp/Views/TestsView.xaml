<UserControl x:Class="EmployeeSurvey.AdminApp.Views.TestsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d" d:DesignHeight="450" d="DesignWidth="800">
    <DockPanel Margin="20">
        <TextBlock DockPanel.Dock="Top" Text="Test Management" FontSize="24" FontWeight="Bold" Foreground="#1E293B"/>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,15">
            <Button Content="Create New Test" Padding="15,8" Background="#0EA5E9" Foreground="White" BorderThickness="0" Command="{Binding CreateNewCommand}"/>
            <Button Content="Auto-Generate" Padding="15,8" Margin="10,0" Background="#8B5CF6" Foreground="White" BorderThickness="0" Command="{Binding NavigateToAutoGenerateCommand}"/>
            <Button Content="Refresh" Padding="15,8" Margin="10,0" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" Command="{Binding LoadDataCommand}"/>
            <Button Content="Edit" Padding="15,8" Margin="10,0" Background="White" BorderBrush="#E2E8F0" BorderThickness="1"
                    Command="{Binding EditCommand}" CommandParameter="{Binding SelectedTest}"/>
            <Button Content="Delete" Padding="15,8" Margin="10,0" Background="#EF4444" Foreground="White" BorderThickness="0"
                    Command="{Binding DeleteCommand}" CommandParameter="{Binding SelectedTest}"/>
        </StackPanel>

        <Border Background="White" CornerRadius="8" Padding="10">
            <DataGrid ItemsSource="{Binding Tests}" SelectedItem="{Binding SelectedTest}" 
                      AutoGenerateColumns="False" 
                      HeadersVisibility="Column" Background="Transparent" BorderThickness="0"
                      CanUserAddRows="False" IsReadOnly="True" GridLinesVisibility="None">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100"/>
                    <DataGridTextColumn Header="Duration" Binding="{Binding DurationMinutes}" Width="80"/>
                    <DataGridCheckBoxColumn Header="Published" Binding="{Binding IsPublished}" Width="80"/>
                    <DataGridTextColumn Header="Created" Binding="{Binding CreatedAt, StringFormat=d}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </DockPanel>
</UserControl>
