<Window x:Class="UniTestSystem.AdminApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UniTestSystem.AdminApp"
        mc:Ignorable="d"
        Title="UniTestSystem Admin" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource AppForegroundBrush}">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <SolidColorBrush x:Key="AppBackgroundBrush" Color="#F4F7FB"/>
        <SolidColorBrush x:Key="AppForegroundBrush" Color="#334155"/>
        <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#FFFFFF"/>

        <Style x:Key="SidebarNavButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="#E5EEFF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="NavRoot"
                                CornerRadius="4"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="NavRoot" Property="Background" Value="#4A6CA8"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="NavRoot" Property="Opacity" Value="0.86"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="SidebarColumn" Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Row="0" Grid.Column="1" Margin="10,10,10,8" Background="{DynamicResource PanelBackgroundBrush}" CornerRadius="8" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="☰" Width="40" Height="36" Click="SidebarToggle_Click"/>
                <TextBox Grid.Column="1" Margin="12,0" Height="36" VerticalContentAlignment="Center" Text="Search modules, schedules, users..." Foreground="#64748B"/>
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{Binding CurrentUserName}" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <Border Background="#ECF3FF" CornerRadius="999" Padding="10,4">
                        <TextBlock Text="{Binding CurrentUserRole}" Foreground="#365FA8" FontWeight="SemiBold"/>
                    </Border>
                    <Button Content="Logout" Margin="10,0,0,0" Height="32" Click="Logout_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="0" Grid.RowSpan="2" Grid.Column="0" Margin="10" Background="#365FA8" CornerRadius="8" Padding="10" x:Name="SidebarPanel">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="UniTest Admin" Foreground="White" FontSize="18" FontWeight="Bold" Margin="8,6,8,16"/>
                    <Button Content="Dashboard" Style="{StaticResource SidebarNavButton}" Tag="0" Click="NavigateTab_Click"/>
                    <Button Content="Tests" Style="{StaticResource SidebarNavButton}" Tag="1" Click="NavigateTab_Click"/>
                    <Button Content="Questions" Style="{StaticResource SidebarNavButton}" Tag="2" Click="NavigateTab_Click"/>
                    <Button Content="Users &amp; Roles" Style="{StaticResource SidebarNavButton}" Tag="3" Click="NavigateTab_Click" Visibility="{Binding CanManageUsersRoles, Converter={StaticResource BoolToVis}}"/>
                    <Button Content="Sessions" Style="{StaticResource SidebarNavButton}" Tag="4" Click="NavigateTab_Click" Visibility="{Binding CanManageAcademic, Converter={StaticResource BoolToVis}}"/>
                    <Button Content="Faculties" Style="{StaticResource SidebarNavButton}" Tag="5" Click="NavigateTab_Click" Visibility="{Binding CanManageAcademic, Converter={StaticResource BoolToVis}}"/>
                    <Button Content="Classes" Style="{StaticResource SidebarNavButton}" Tag="6" Click="NavigateTab_Click" Visibility="{Binding CanManageAcademic, Converter={StaticResource BoolToVis}}"/>
                    <Button Content="Courses" Style="{StaticResource SidebarNavButton}" Tag="7" Click="NavigateTab_Click" Visibility="{Binding CanManageAcademic, Converter={StaticResource BoolToVis}}"/>
                    <Button Content="Enrollments" Style="{StaticResource SidebarNavButton}" Tag="8" Click="NavigateTab_Click" Visibility="{Binding CanManageAcademic, Converter={StaticResource BoolToVis}}"/>
                    <Button Content="Audit Log" Style="{StaticResource SidebarNavButton}" Tag="9" Click="NavigateTab_Click" Visibility="{Binding CanViewAudit, Converter={StaticResource BoolToVis}}"/>
                    <Button Content="Exam Scheduling" Style="{StaticResource SidebarNavButton}" Tag="10" Click="NavigateTab_Click" Visibility="{Binding CanManageScheduling, Converter={StaticResource BoolToVis}}"/>
                    <Button Content="Settings" Style="{StaticResource SidebarNavButton}" Tag="11" Click="NavigateTab_Click" Visibility="{Binding CanViewSettings, Converter={StaticResource BoolToVis}}"/>
                    <Button Content="Backup/Restore" Style="{StaticResource SidebarNavButton}" Tag="12" Click="NavigateTab_Click" Visibility="{Binding CanViewMaintenance, Converter={StaticResource BoolToVis}}"/>
                    <Button Content="Year-End" Style="{StaticResource SidebarNavButton}" Tag="13" Click="NavigateTab_Click" Visibility="{Binding CanManageAcademic, Converter={StaticResource BoolToVis}}"/>
                    <Separator Margin="6,10"/>
                    <Button Content="Refresh Data" Style="{StaticResource SidebarNavButton}" Command="{Binding LoadDataCommand}"/>
                    <Button Content="Toggle Theme" Style="{StaticResource SidebarNavButton}" Command="{Binding ToggleThemeCommand}"/>
                    <Button Content="Logout" Style="{StaticResource SidebarNavButton}" Click="Logout_Click"/>
                    <Button Content="Exit" Style="{StaticResource SidebarNavButton}" Click="Exit_Click"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Main Content -->
        <TabControl Grid.Row="1" Grid.Column="1" Margin="10,0,10,10" x:Name="MainTabControl" Background="{DynamicResource PanelBackgroundBrush}" SelectionChanged="MainTabControl_SelectionChanged">
            <!-- Dashboard Tab -->
            <TabItem Header="Dashboard">
                <ScrollViewer>
                    <StackPanel Margin="20">
                        <TextBlock Text="System Overview" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
                        <UniformGrid Columns="4">
                            <Border Background="#2196F3" CornerRadius="5" Margin="5" Height="100">
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding Stats.TotalUsers}" FontSize="32" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Total Students" Foreground="White" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <Border Background="#4CAF50" CornerRadius="5" Margin="5" Height="100">
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding Stats.TotalTests}" FontSize="32" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Total Tests" Foreground="White" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <Border Background="#FF9800" CornerRadius="5" Margin="5" Height="100">
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding Stats.TotalQuestions}" FontSize="32" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Total Questions" Foreground="White" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <Border Background="#9C27B0" CornerRadius="5" Margin="5" Height="100">
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding Stats.TotalSubmissions}" FontSize="32" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Total Submissions" Foreground="White" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </UniformGrid>
                        
                        <TextBlock Text="Recent Activity" FontSize="18" FontWeight="Bold" Margin="0,20,0,10"/>
                        <DataGrid ItemsSource="{Binding Sessions}" AutoGenerateColumns="False" IsReadOnly="True" Height="200">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Student" Binding="{Binding UserName}" Width="150"/>
                                <DataGridTextColumn Header="Test" Binding="{Binding TestTitle}" Width="*"/>
                                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                                <DataGridTextColumn Header="Score" Binding="{Binding TotalScore}" Width="60"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Tests Tab -->
            <TabItem Header="Tests">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <DataGrid Grid.Column="0" ItemsSource="{Binding Tests}" SelectedItem="{Binding SelectedTest}" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                            <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100"/>
                            <DataGridTextColumn Header="Duration" Binding="{Binding DurationMinutes}" Width="80"/>
                            <DataGridTextColumn Header="Max Score" Binding="{Binding TotalMaxScore}" Width="80"/>
                            <DataGridCheckBoxColumn Header="Pub" Binding="{Binding IsPublished}" Width="40"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <GroupBox Grid.Column="1" Header="Test Details" Margin="10">
                        <ScrollViewer>
                            <Grid Margin="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="Title:"/>
                                <TextBox Grid.Row="1" Text="{Binding SelectedTest.Title}" Margin="0,0,0,10"/>

                                <TextBlock Grid.Row="2" Text="Type:"/>
                                <ComboBox Grid.Row="3" Text="{Binding SelectedTest.Type}" Margin="0,0,0,10">
                                    <ComboBoxItem Content="Test"/>
                                    <ComboBoxItem Content="Exam"/>
                                </ComboBox>

                                <TextBlock Grid.Row="4" Text="Duration (min):"/>
                                <TextBox Grid.Row="5" Text="{Binding SelectedTest.DurationMinutes}" Margin="0,0,0,10"/>

                                <TextBlock Grid.Row="6" Text="Max Score:"/>
                                <TextBox Grid.Row="7" Text="{Binding SelectedTest.TotalMaxScore}" Margin="0,0,0,10"/>

                                <CheckBox Grid.Row="8" Content="Is Published" IsChecked="{Binding SelectedTest.IsPublished}" Margin="0,0,0,10"/>

                                <TextBlock Grid.Row="9" Text="Test Questions:" FontWeight="Bold" Margin="0,10,0,5"/>
                                <DataGrid Grid.Row="10" ItemsSource="{Binding SelectedTestItems}" SelectedItem="{Binding SelectedTestItem}" AutoGenerateColumns="False" Height="150" Margin="0,0,0,10">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Question ID" Binding="{Binding QuestionId}" Width="*"/>
                                        <DataGridTextColumn Header="Points" Binding="{Binding Points}" Width="60"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                                
                                <StackPanel Grid.Row="11" Orientation="Horizontal" Margin="0,0,0,10">
                                    <Button Content="Add Question" Command="{Binding AddTestItemCommand}" Width="100" Margin="0,0,5,0"/>
                                    <Button Content="Remove" Command="{Binding RemoveTestItemCommand}" Width="70"/>
                                </StackPanel>

                                <StackPanel Grid.Row="12" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0" Visibility="{Binding CanManageSystem, Converter={StaticResource BoolToVis}}">
                                    <Button Content="Save" Command="{Binding SaveTestCommand}" Width="70" Margin="0,0,5,0" Background="#4CAF50" Foreground="White"/>
                                    <Button Content="Delete" Command="{Binding DeleteTestCommand}" Width="70"/>
                                </StackPanel>
                            </Grid>
                        </ScrollViewer>
                    </GroupBox>
                </Grid>
            </TabItem>

            <!-- Questions Tab -->
            <TabItem Header="Questions">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <DataGrid Grid.Column="0" ItemsSource="{Binding Questions}" SelectedItem="{Binding SelectedQuestion}" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Content" Binding="{Binding Content}" Width="*"/>
                            <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="80"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80"/>
                            <DataGridTextColumn Header="Skill" Binding="{Binding Skill}" Width="100"/>
                            <DataGridTextColumn Header="Difficulty" Binding="{Binding Difficulty}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <GroupBox Grid.Column="1" Header="Question Details" Margin="10">
                        <ScrollViewer>
                            <Grid Margin="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="Content:"/>
                                <TextBox Grid.Row="1" Text="{Binding SelectedQuestion.Content}" AcceptsReturn="True" Height="60" TextWrapping="Wrap" Margin="0,0,0,10"/>

                                <TextBlock Grid.Row="2" Text="Type:"/>
                                <ComboBox Grid.Row="3" Text="{Binding SelectedQuestion.Type}" Margin="0,0,0,10">
                                    <ComboBoxItem Content="MCQ"/>
                                    <ComboBoxItem Content="TrueFalse"/>
                                    <ComboBoxItem Content="Essay"/>
                                    <ComboBoxItem Content="Matching"/>
                                    <ComboBoxItem Content="DragDrop"/>
                                </ComboBox>

                                <TextBlock Grid.Row="4" Text="Skill:"/>
                                <TextBox Grid.Row="5" Text="{Binding SelectedQuestion.Skill}" Margin="0,0,0,10"/>

                                <TextBlock Grid.Row="6" Text="Difficulty:"/>
                                <TextBox Grid.Row="7" Text="{Binding SelectedQuestion.Difficulty}" Margin="0,0,0,10"/>

                                <TextBlock Grid.Row="8" Text="Options &amp; Correct Keys:" FontWeight="Bold" Margin="0,10,0,5"/>
                                <ListBox Grid.Row="9" ItemsSource="{Binding SelectedQuestionOptions}" SelectedItem="{Binding SelectedOption}" Height="100" Margin="0,0,0,5">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="200" BorderThickness="0"/>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                                
                                <StackPanel Grid.Row="10" Orientation="Horizontal" Margin="0,0,0,10">
                                    <Button Content="Add Option" Command="{Binding AddOptionCommand}" Width="80" Margin="0,0,5,0"/>
                                    <Button Content="Remove" Command="{Binding RemoveOptionCommand}" Width="60" Margin="0,0,5,0"/>
                                    <Button Content="Set Correct" Command="{Binding ToggleCorrectKeyCommand}" Width="80" Background="#FFF9C4"/>
                                </StackPanel>

                                <TextBlock Grid.Row="11" Text="Correct Keys:" FontSize="11" Foreground="Gray"/>
                                <ItemsControl Grid.Row="12" ItemsSource="{Binding SelectedQuestionCorrectKeys}" Margin="0,0,0,10">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}" FontStyle="Italic" Margin="0,2,0,2"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <StackPanel Grid.Row="13" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0" Visibility="{Binding CanManageSystem, Converter={StaticResource BoolToVis}}">
                                    <Button Content="Save" Command="{Binding SaveQuestionCommand}" Width="70" Margin="0,0,5,0" Background="#4CAF50" Foreground="White"/>
                                    <Button Content="Submit" Command="{Binding SubmitQuestionCommand}" Width="70" Margin="0,0,5,0" Background="#2196F3" Foreground="White"/>
                                    <Button Content="Approve" Command="{Binding ApproveQuestionCommand}" Width="70" Margin="0,0,5,0" Background="#4CAF50" Foreground="White"/>
                                    <Button Content="Reject" Command="{Binding RejectQuestionCommand}" Width="70" Margin="0,0,5,0" Background="#F44336" Foreground="White"/>
                                    <Button Content="Delete" Command="{Binding DeleteQuestionCommand}" Width="70"/>
                                </StackPanel>
                            </Grid>
                        </ScrollViewer>
                    </GroupBox>
                </Grid>
            </TabItem>

            <!-- Users & Roles Tab -->
            <TabItem Header="Users &amp; Roles" Visibility="{Binding CanManageUsersRoles, Converter={StaticResource BoolToVis}}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <DataGrid Grid.Column="0" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                            <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="80"/>
                            <DataGridTextColumn Header="Faculty" Binding="{Binding Department}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <GroupBox Grid.Column="1" Header="User Details" Margin="10">
                        <ScrollViewer>
                            <Grid Margin="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="Name:"/>
                                <TextBox Grid.Row="1" Text="{Binding SelectedUser.Name}" Margin="0,0,0,10"/>

                                <TextBlock Grid.Row="2" Text="Email:"/>
                                <TextBox Grid.Row="3" Text="{Binding SelectedUser.Email}" Margin="0,0,0,10"/>

                                <TextBlock Grid.Row="4" Text="Role:"/>
                                <ComboBox Grid.Row="5" SelectedItem="{Binding SelectedUser.Role}" ItemsSource="{Binding EditableRoles}" Margin="0,0,0,10"/>

                                <TextBlock Grid.Row="6" Text="Faculty:"/>
                                <TextBox Grid.Row="7" Text="{Binding SelectedUser.Department}" Margin="0,0,0,10"/>
                                
                                <TextBlock Grid.Row="8" Text="Skill:"/>
                                <TextBox Grid.Row="9" Text="{Binding SelectedUser.Skill}" Margin="0,0,0,10"/>

                                <TextBlock Grid.Row="10" Text="Level:"/>
                                <ComboBox Grid.Row="11" Text="{Binding SelectedUser.Level}" Margin="0,0,0,10">
                                    <ComboBoxItem Content="Junior"/>
                                    <ComboBoxItem Content="Mid"/>
                                    <ComboBoxItem Content="Senior"/>
                                </ComboBox>

                                <TextBlock Grid.Row="12" Text="Class ID:"/>
                                <TextBox Grid.Row="13" Text="{Binding SelectedUser.TeamId}" Margin="0,0,0,10"/>

                                <StackPanel Grid.Row="14" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                                    <Button Content="Save" Command="{Binding SaveUserCommand}" Width="70" Margin="0,0,5,0" Background="#4CAF50" Foreground="White"/>
                                    <Button Content="Delete" Command="{Binding DeleteUserCommand}" Width="70"/>
                                </StackPanel>
                            </Grid>
                        </ScrollViewer>
                    </GroupBox>
                </Grid>
            </TabItem>

            <!-- Sessions Tab -->
            <TabItem Header="Sessions" Visibility="{Binding CanManageAcademic, Converter={StaticResource BoolToVis}}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <DataGrid Grid.Row="0" ItemsSource="{Binding Sessions}" SelectedItem="{Binding SelectedSession}" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Student" Binding="{Binding UserName}" Width="150"/>
                            <DataGridTextColumn Header="Test" Binding="{Binding TestTitle}" Width="*"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                            <DataGridTextColumn Header="Score" Binding="{Binding TotalScore}" Width="60"/>
                            <DataGridTextColumn Header="Max" Binding="{Binding MaxScore}" Width="60"/>
                            <DataGridTextColumn Header="%" Binding="{Binding Percent, StringFormat=\{0:N1\}}" Width="60"/>
                            <DataGridCheckBoxColumn Header="Pass" Binding="{Binding IsPassed}" Width="50"/>
                            <DataGridTextColumn Header="Last Activity" Binding="{Binding LastActivityAt, StringFormat=\{0:yyyy-MM-dd HH:mm\}}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
                        <Button Content="Terminate &amp; Delete Session" Command="{Binding DeleteSessionCommand}" Width="180" Height="30"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- Academic Management - Faculties -->
            <TabItem Header="Faculties" Visibility="{Binding CanManageAcademic, Converter={StaticResource BoolToVis}}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="320"/>
                    </Grid.ColumnDefinitions>

                    <DataGrid Grid.Column="0" ItemsSource="{Binding Faculties}" SelectedItem="{Binding SelectedFaculty}" AutoGenerateColumns="False" IsReadOnly="True" Margin="5">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="100"/>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="1.5*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <ScrollViewer Grid.Column="1" Margin="5">
                        <StackPanel>
                            <TextBlock Text="Faculty Management" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                            <Button Content="+ New Faculty" Command="{Binding AddFacultyCommand}" HorizontalAlignment="Left" Margin="0,0,0,15"/>
                            
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Label Content="Name:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SelectedFaculty.Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                                <Label Content="Code:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SelectedFaculty.Code, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                                <Label Content="Description:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Top"/>
                                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding SelectedFaculty.Description, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" AcceptsReturn="True" Height="100" Margin="0,0,0,10"/>

                                <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                                    <Button Content="Save" Command="{Binding SaveFacultyCommand}" Width="80" Margin="0,0,5,0" Background="#4CAF50" Foreground="White" BorderThickness="0"/>
                                    <Button Content="Delete" Command="{Binding DeleteFacultyCommand}" Width="80"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </TabItem>

            <!-- Academic Management - Classes -->
            <TabItem Header="Classes" Visibility="{Binding CanManageAcademic, Converter={StaticResource BoolToVis}}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="320"/>
                    </Grid.ColumnDefinitions>

                    <DataGrid Grid.Column="0" ItemsSource="{Binding Classes}" SelectedItem="{Binding SelectedClass}" AutoGenerateColumns="False" IsReadOnly="True" Margin="5">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="100"/>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <ScrollViewer Grid.Column="1" Margin="5">
                        <StackPanel>
                            <TextBlock Text="Class Management" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                            <Button Content="+ New Class" Command="{Binding AddClassCommand}" HorizontalAlignment="Left" Margin="0,0,0,15"/>
                            
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Label Content="Name:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SelectedClass.Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                                <Label Content="Code:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SelectedClass.Code, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                                <Label Content="Faculty:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
                                <ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding Faculties}" SelectedValue="{Binding SelectedClass.FacultyId}" SelectedValuePath="Id" DisplayMemberPath="Name" Margin="0,0,0,10"/>

                                <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                                    <Button Content="Save" Command="{Binding SaveClassCommand}" Width="80" Margin="0,0,5,0" Background="#4CAF50" Foreground="White" BorderThickness="0"/>
                                    <Button Content="Delete" Command="{Binding DeleteClassCommand}" Width="80"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </TabItem>

            <!-- Courses Tab -->
            <TabItem Header="Courses" Visibility="{Binding CanManageAcademic, Converter={StaticResource BoolToVis}}">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="320"/>
                    </Grid.ColumnDefinitions>

                    <DockPanel Grid.Column="0">
                        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
                            <Button Content="Add Course" Command="{Binding AddCourseCommand}" Width="100" Margin="0,0,10,0"/>
                            <Button Content="Import Courses" Command="{Binding ImportCoursesCommand}" Width="120" Margin="0,0,10,0"/>
                            <Button Content="Refresh" Command="{Binding LoadDataCommand}" Width="80"/>
                        </StackPanel>
                        <DataGrid ItemsSource="{Binding Courses}" SelectedItem="{Binding SelectedCourse}" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="100"/>
                                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="Credits" Binding="{Binding Credits}" Width="60"/>
                                <DataGridTextColumn Header="Area" Binding="{Binding SubjectArea}" Width="150"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>

                    <ScrollViewer Grid.Column="1" Margin="10,0,0,0">
                        <StackPanel>
                            <TextBlock Text="Course Details" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Label Content="Name:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SelectedCourse.Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                                <Label Content="Code:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SelectedCourse.Code, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                                <Label Content="Credits:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding SelectedCourse.Credits, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                                <Label Content="Area:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center"/>
                                <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding SelectedCourse.SubjectArea, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                                <Label Content="Lecturer:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center"/>
                                <ComboBox Grid.Row="4" Grid.Column="1" ItemsSource="{Binding Lecturers}" SelectedValue="{Binding SelectedCourse.LecturerId}" SelectedValuePath="Id" DisplayMemberPath="Name" Margin="0,0,0,10"/>

                                <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                                    <Button Content="Save" Command="{Binding SaveCourseCommand}" Width="80" Margin="0,0,5,0" Background="#4CAF50" Foreground="White" BorderThickness="0"/>
                                    <Button Content="Delete" Command="{Binding DeleteCourseCommand}" Width="80"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </TabItem>

            <!-- Enrollments Tab -->
            <TabItem Header="Enrollments" Visibility="{Binding CanManageAcademic, Converter={StaticResource BoolToVis}}">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <DockPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock DockPanel.Dock="Top" Text="Select Course" FontWeight="Bold" Margin="0,0,0,5"/>
                        <DataGrid ItemsSource="{Binding Courses}" SelectedItem="{Binding SelectedCourse}" AutoGenerateColumns="False" IsReadOnly="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="100"/>
                                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>

                    <DockPanel Grid.Column="1">
                        <TextBlock DockPanel.Dock="Top" Text="Enrolled Students" FontWeight="Bold" Margin="0,0,0,5"/>
                        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
                            <Button Content="Import Students" Command="{Binding ImportStudentsCommand}" Width="120" Margin="0,0,10,0"/>
                            <Button Content="Unenroll" Command="{Binding UnenrollStudentCommand}" Width="100"/>
                        </StackPanel>
                        <DataGrid ItemsSource="{Binding Enrollments}" SelectedItem="{Binding SelectedEnrollment}" AutoGenerateColumns="False" IsReadOnly="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Student ID" Binding="{Binding StudentId}" Width="*"/>
                                <DataGridTextColumn Header="Name" Binding="{Binding StudentName}" Width="*"/>
                                <DataGridTextColumn Header="Semester" Binding="{Binding Semester}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>
                </Grid>
            </TabItem>

            <!-- Audit Log Tab -->
            <TabItem Header="Audit Log" Visibility="{Binding CanViewAudit, Converter={StaticResource BoolToVis}}">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <WrapPanel Grid.Row="0" Margin="0,0,0,10">
                        <TextBlock Text="From" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBox Width="120" Text="{Binding AuditFrom, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0"/>
                        <TextBlock Text="To" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBox Width="120" Text="{Binding AuditTo, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0"/>
                        <TextBlock Text="Actor" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBox Width="140" Text="{Binding AuditActor, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0"/>
                        <TextBlock Text="Keyword" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBox Width="180" Text="{Binding AuditKeyword, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0"/>
                        <Button Content="Load Audit" Command="{Binding LoadAuditCommand}" Width="100"/>
                    </WrapPanel>
                    <DataGrid Grid.Row="1" ItemsSource="{Binding AuditLogs}" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="At (UTC)" Binding="{Binding At, StringFormat=\{0:yyyy-MM-dd HH:mm:ss\}}" Width="160"/>
                            <DataGridTextColumn Header="Actor" Binding="{Binding Actor}" Width="160"/>
                            <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="160"/>
                            <DataGridTextColumn Header="Entity" Binding="{Binding EntityName}" Width="140"/>
                            <DataGridTextColumn Header="Entity ID" Binding="{Binding EntityId}" Width="180"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- Exam Scheduling Tab -->
            <TabItem Header="Exam Scheduling" Visibility="{Binding CanManageScheduling, Converter={StaticResource BoolToVis}}">
                <Grid Margin="14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" Margin="0,0,12,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Background="{DynamicResource PanelBackgroundBrush}" Padding="12" CornerRadius="8" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="1.2*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" Margin="0,0,10,0" Text="Search by test title, course, room..." Foreground="#64748B" VerticalContentAlignment="Center"/>
                                <ComboBox Grid.Column="1" Margin="0,0,10,0">
                                    <ComboBoxItem Content="All Types" IsSelected="True"/>
                                    <ComboBoxItem Content="Midterm"/>
                                    <ComboBoxItem Content="Final"/>
                                    <ComboBoxItem Content="Quiz"/>
                                </ComboBox>
                                <Button Grid.Column="2" Content="Refresh" Command="{Binding LoadDataCommand}" Width="90"/>
                            </Grid>
                        </Border>

                        <DataGrid Grid.Row="1" ItemsSource="{Binding ExamSchedules}" SelectedItem="{Binding SelectedExamSchedule}" AutoGenerateColumns="False" IsReadOnly="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Course" Binding="{Binding Course.Name}" Width="*"/>
                                <DataGridTextColumn Header="Test" Binding="{Binding Test.Title}" Width="*"/>
                                <DataGridTextColumn Header="Room" Binding="{Binding Room}" Width="110"/>
                                <DataGridTextColumn Header="Start" Binding="{Binding StartTime, StringFormat=\{0:yyyy-MM-dd HH:mm\}}" Width="160"/>
                                <DataGridTextColumn Header="End" Binding="{Binding EndTime, StringFormat=\{0:yyyy-MM-dd HH:mm\}}" Width="160"/>
                                <DataGridTextColumn Header="Type" Binding="{Binding ExamType}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <Grid Grid.Row="2" Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Orientation="Horizontal">
                                <Button Content="Create Single" Command="{Binding CreateSingleScheduleCommand}" Width="110" Margin="0,0,8,0"/>
                                <Button Content="Create Bulk" Command="{Binding CreateBulkScheduleCommand}" Width="110" Margin="0,0,8,0"/>
                                <Button Content="Export CSV" Command="{Binding ExportScheduleCsvCommand}" Width="100" Margin="0,0,8,0"/>
                                <Button Content="Delete Selected" Command="{Binding DeleteScheduleCommand}" Width="120"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                <Button Content="&lt;" Width="32" Margin="0,0,6,0"/>
                                <TextBlock Text="Page 1 / 1" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <Button Content="&gt;" Width="32"/>
                            </StackPanel>
                        </Grid>
                    </Grid>

                    <Border Grid.Column="1" Background="{DynamicResource PanelBackgroundBrush}" CornerRadius="8" Padding="14">
                        <ScrollViewer>
                            <StackPanel>
                                <TextBlock Text="Exam Schedule Form" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>

                                <TextBlock Text="Test" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                <ComboBox ItemsSource="{Binding Tests}" SelectedValuePath="Id" DisplayMemberPath="Title" SelectedValue="{Binding SelectedScheduleTestId}" Margin="0,0,0,10"/>

                                <TextBlock Text="Course" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                <ComboBox ItemsSource="{Binding Courses}" SelectedValuePath="Id" DisplayMemberPath="Name" SelectedValue="{Binding SelectedScheduleCourseId}" Margin="0,0,0,10"/>

                                <TextBlock Text="Room" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                <TextBox Text="{Binding SelectedScheduleRoom, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                                <TextBlock Text="Start Time" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                <TextBox Text="{Binding SelectedScheduleStartTime, StringFormat=\{0:yyyy-MM-dd HH:mm\}, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                                <TextBlock Text="End Time" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                <TextBox Text="{Binding SelectedScheduleEndTime, StringFormat=\{0:yyyy-MM-dd HH:mm\}, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                                <TextBlock Text="Exam Type" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                <ComboBox SelectedValue="{Binding SelectedScheduleExamType}" Margin="0,0,0,12">
                                    <ComboBoxItem Content="Midterm"/>
                                    <ComboBoxItem Content="Final"/>
                                    <ComboBoxItem Content="Quiz"/>
                                </ComboBox>

                                <WrapPanel Margin="0,0,0,10">
                                    <Button Content="+ Add To Queue" Command="{Binding AddScheduleDraftCommand}" Width="130" Margin="0,0,8,0"/>
                                    <Button Content="Remove Queue Item" Command="{Binding RemoveScheduleDraftCommand}" Width="140"/>
                                </WrapPanel>

                                <TextBlock Text="Queue" FontWeight="Bold" Margin="0,6,0,6"/>
                                <DataGrid ItemsSource="{Binding ScheduleDrafts}" SelectedItem="{Binding SelectedScheduleDraft}" AutoGenerateColumns="False" Height="210">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="CourseId" Binding="{Binding CourseId}" Width="*"/>
                                        <DataGridTextColumn Header="TestId" Binding="{Binding TestId}" Width="*"/>
                                        <DataGridTextColumn Header="Room" Binding="{Binding Room}" Width="90"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </TabItem>

            <!-- Settings Tab -->
            <TabItem Header="Settings" Visibility="{Binding CanViewSettings, Converter={StaticResource BoolToVis}}">
                <Grid Margin="20">
                    <StackPanel MaxWidth="420" HorizontalAlignment="Left">
                        <TextBlock Text="System Settings" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

                        <Label Content="System Name"/>
                        <TextBox Text="{Binding SystemSettings.SystemName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                        <Label Content="Current Semester"/>
                        <TextBox Text="{Binding SystemSettings.CurrentSemester, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                        <Label Content="Current Academic Year"/>
                        <TextBox Text="{Binding SystemSettings.CurrentAcademicYear, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                        <Label Content="Warning GPA Threshold"/>
                        <TextBox Text="{Binding SystemSettings.WarningGpaThreshold, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                        <Label Content="Fail GPA Threshold"/>
                        <TextBox Text="{Binding SystemSettings.FailGpaThreshold, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                        <CheckBox Content="Treat Outstanding Debt As Fail" IsChecked="{Binding SystemSettings.TreatOutstandingDebtAsFail}" Margin="0,0,0,10"/>

                        <Label Content="Logo URL"/>
                        <TextBox Text="{Binding SystemSettings.LogoUrl, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,20"/>

                        <WrapPanel>
                            <Button Content="Save Settings" Command="{Binding SaveSettingsCommand}" Width="120" Height="30" Background="#2196F3" Foreground="White" BorderThickness="0" Margin="0,0,8,0"/>
                            <ComboBox Width="120" ItemsSource="{Binding PeriodOptions}" SelectedItem="{Binding SelectedExportPeriod}" Margin="0,0,8,0"/>
                            <ComboBox Width="90" ItemsSource="{Binding ExportFormatOptions}" SelectedItem="{Binding SelectedExportFormat}" Margin="0,0,8,0"/>
                            <Button Content="Export Periodic" Command="{Binding ExportPeriodicReportCommand}" Width="120" Height="30"/>
                        </WrapPanel>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- Maintenance Tab -->
            <TabItem Header="Backup/Restore" Visibility="{Binding CanViewMaintenance, Converter={StaticResource BoolToVis}}">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <Button Content="Create Backup" Command="{Binding CreateBackupCommand}" Width="130" Margin="0,0,8,0"/>
                        <Button Content="Restore From File" Command="{Binding RestoreBackupCommand}" Width="140"/>
                    </StackPanel>
                    <DataGrid Grid.Row="1" ItemsSource="{Binding BackupFiles}" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="File" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Size (bytes)" Binding="{Binding Length}" Width="150"/>
                            <DataGridTextColumn Header="LastWriteUtc" Binding="{Binding LastWriteTimeUtc, StringFormat=\{0:yyyy-MM-dd HH:mm:ss\}}" Width="180"/>
                            <DataGridTextColumn Header="Path" Binding="{Binding FullName}" Width="2*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- Year-End Tab -->
            <TabItem Header="Year-End" Visibility="{Binding CanManageAcademic, Converter={StaticResource BoolToVis}}">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <WrapPanel Grid.Row="0" Margin="0,0,0,10">
                        <TextBlock Text="Academic Year" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox Width="140" Text="{Binding YearEndAcademicYear, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
                        <TextBlock Text="Faculty" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox Width="240" ItemsSource="{Binding Faculties}" SelectedValuePath="Id" DisplayMemberPath="Name" SelectedValue="{Binding YearEndFacultyId}" Margin="0,0,10,0"/>
                        <Button Content="Preview" Command="{Binding PreviewYearEndCommand}" Width="90" Margin="0,0,8,0"/>
                        <Button Content="Finalize" Command="{Binding FinalizeYearEndCommand}" Width="100" Background="#DC2626" Foreground="White" BorderThickness="0"/>
                    </WrapPanel>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                        <Border Background="#E2E8F0" CornerRadius="4" Padding="8" Margin="0,0,8,0">
                            <TextBlock Text="{Binding YearEndPreview.TotalStudents, StringFormat=Students: {0}}"/>
                        </Border>
                        <Border Background="#FEE2E2" CornerRadius="4" Padding="8" Margin="0,0,8,0">
                            <TextBlock Text="{Binding YearEndWarningStudents.Count, StringFormat=Warning/Fail: {0}}"/>
                        </Border>
                        <Border Background="#DBEAFE" CornerRadius="4" Padding="8">
                            <TextBlock Text="{Binding YearEndPreview.Prerequisites.IsReady, StringFormat=Ready: {0}}"/>
                        </Border>
                    </StackPanel>

                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <DataGrid Grid.Column="0" ItemsSource="{Binding YearEndStudents}" AutoGenerateColumns="False" IsReadOnly="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Student" Binding="{Binding StudentName}" Width="*"/>
                                <DataGridTextColumn Header="ID" Binding="{Binding StudentId}" Width="130"/>
                                <DataGridTextColumn Header="Faculty" Binding="{Binding FacultyName}" Width="150"/>
                                <DataGridTextColumn Header="Class" Binding="{Binding ClassName}" Width="120"/>
                                <DataGridTextColumn Header="GPA 4.0" Binding="{Binding YearEndGpa4, StringFormat=\{0:0.00\}}" Width="90"/>
                                <DataGridTextColumn Header="GPA 10" Binding="{Binding YearEndGpa10, StringFormat=\{0:0.00\}}" Width="90"/>
                                <DataGridTextColumn Header="Credits" Binding="{Binding TotalCreditsEarned}" Width="80"/>
                                <DataGridTextColumn Header="Status" Binding="{Binding AcademicStatus}" Width="90"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="Prerequisites" FontWeight="Bold" Margin="0,0,0,8"/>
                            <TextBlock Text="{Binding YearEndPreview.Prerequisites.AllExamSchedulesCompleted, StringFormat=Exam schedules completed: {0}}" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding YearEndPreview.Prerequisites.AllEssaysGraded, StringFormat=Essays graded: {0}}" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding YearEndPreview.Prerequisites.SemesterTranscriptsLocked, StringFormat=Semester locked: {0}}" Margin="0,0,0,8"/>
                            <TextBlock Text="Missing items" FontWeight="Bold" Margin="0,8,0,6"/>
                            <ItemsControl ItemsSource="{Binding YearEndPreview.Prerequisites.MissingItems}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="0,0,0,4"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <TextBlock Text="Warning/Fail Students" FontWeight="Bold" Margin="0,10,0,6"/>
                            <DataGrid ItemsSource="{Binding YearEndWarningStudents}" AutoGenerateColumns="False" IsReadOnly="True" Height="220">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Student" Binding="{Binding StudentName}" Width="*"/>
                                    <DataGridTextColumn Header="GPA" Binding="{Binding YearEndGpa4, StringFormat=\{0:0.00\}}" Width="70"/>
                                    <DataGridTextColumn Header="Status" Binding="{Binding AcademicStatus}" Width="80"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Grid>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2" Grid.ColumnSpan="2">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
